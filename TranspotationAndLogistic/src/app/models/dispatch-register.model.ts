// Create this new model file for the dispatch data structure
export interface Dispatch {
  id: string;
  dispatchNo: number;
  gardenMark: string;
  driverNic: string;
  vehicleNumber: string;
  bagCount: number;
  dispatchDate: string;
  sealNumber: string;
}