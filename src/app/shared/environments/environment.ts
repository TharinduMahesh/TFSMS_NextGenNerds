export const environment = {
  production: false,
  apiBaseUrl: 'https://localhost:7197',
  // Set this to true to bypass SSL certificate validation (for development only)
  bypassSSLValidation: true
};

// https://localhost:7203